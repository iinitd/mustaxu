extends ../layout

block content
	.container
		.row
			h1 #{article.title}
			h4 #{article.author}
		.row
			each u in article.tags
				a(href="/tag/#{u}") #{u} 
			- if (user)
				a(href="/edit/#{article._id}") edit
				a 
				a(href="/remove/#{article._id}") remove
		.row
			!{html}